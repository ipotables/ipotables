extends layout

block mainContent
  h2 Create Thing
  - if(error != null) {
  div(class="alert alert-warning alert-dismissable")
    button(type="button", class="close", data-dismiss="alert", aria-hidden="true") &times;
    span= error.message
  - }
  form(action="/thing/create", method="POST",  data-ajax="false", role="form")
    include thing_form
    input.submit(type="submit", value="Create!", class="btn btn-primary")
